logging:
  level:
    org:
      springframework:
        test:
          context:
            cache: DEBUG
    de:
      schulung:
        spring:
          customers:
            shared:
              interceptors: DEBUG
spring:
  jpa:
    show-sql: true
